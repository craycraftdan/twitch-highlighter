import { CreatedPlaylistData } from './fakeCreatedPlaylist';

const CreatedPlaylist = (state = CreatedPlaylistData, action) => {
    switch(action.type) {
        default: 
            return state
    }
}

export default CreatedPlaylist